# ──────────────────────────────────────────────────────────────────────────────
# TEMPLATE – do NOT commit real values.
# Apply real secrets out-of-band with:
#
#   kubectl create secret generic ofertonazos-secrets \
#     --from-literal=SUPABASE_SERVICE_ROLE_KEY=<real-value> \
#     --from-literal=API_SECRET_KEY=<real-value> \
#     --dry-run=client -o yaml | kubectl apply -f -
# ──────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: ofertonazos-secrets
  labels:
    app: ofertonazos
type: Opaque
data:
  # base64-encoded placeholder values — replace before deploying
  SUPABASE_SERVICE_ROLE_KEY: cmVwbGFjZW1l  # echo -n 'replaceme' | base64
  API_SECRET_KEY: cmVwbGFjZW1l             # echo -n 'replaceme' | base64
