apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ofertonazos.fullname" . }}-config
  labels:
    {{- include "ofertonazos.labels" . | nindent 4 }}
data:
  NODE_ENV: {{ .Values.env.NODE_ENV | quote }}
  PORT: {{ .Values.env.PORT | quote }}
